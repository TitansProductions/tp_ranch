# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Added (2) new functions on `client/escrow_ignore.lua` file related to fertilizers for using your own eye-target script.
   
## Modified Files:

- client/fertilizer.lua
- client/escrow_ignore.lua
- fxmanifest.lua

## Notes

1. Add new (2) functions in the mentioned file.

```lua
-- Add the pitchfork object target options for attaching.
function AddPitchForkEntityObjectTargetOptions(ranchName, object) -- 1.0.5
	
	local options = {
		{
			label = '<span style="margin-left: -20px; color: #e4d946de;">' .. Locales['PICKUP_PITCHFORK'] ..'</span>',
			icon = '',
			radius = 1.1,
			distance = 1.1,

			onSelect = function(data)
				OnPitchforkEquip(ranchName)
			end,

		},

	}

	exports.ox_target:addLocalEntity(object, options)
end

-- Removes the pitchfork object options for picking up when rendering off.
function RemovePitchForkEntityObjectTarget(object)-- 1.0.5

	-- If you are using OX_TARGET this is the following export to be used
	-- otherwise remove it and add from the target you are using.
	exports.ox_target:removeLocalEntity(object, nil)
end

```

## How to update?

- You can easily update the modified files by redownloading from the **Cfx.re Portal** and copy-paste on the mentioned directory file paths.
- If new options have been implemented to the configuration file or edited, **DO NOT** copy-paste the new configuration file, otherwise you will get the latest configuration but you will lose all the changes you have made. The proper way updating a configuration file is by finding the new configuration options or the modified ones and do it **manually**.

*Cfx.re Portal **DOES NOT** force-update your configuration file or any files, if it did, you would always lose all the configuration changes you made or would totally break the script, all files are updated or edited manually.*
