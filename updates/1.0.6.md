# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Added objectloader system directly to the ranch script than using **TP Objectloader** as separate script (check notes).
2. Improved LoadModel function which is the most important function for loading objects and made it even more secure for objects that are custom and doesnt exist on your server to not break the script but actual skip those models from loading after some attempts.
3. Added (3) more API exports for the developers.
4. Added new feature to change chicken eggs model with your own desired object.
5. Fixed an issue that would not allow the players to pickup the eggs.
6. Fixed an issue on the menu section related to goats that could not allow breeding.
7. Removed sheep syncing state to a local state and in the next update we are going to sync the animation to rest players as local for the best performance and safety.

## Modified Files:

- client/functions.lua
- client/menu.lua
- client/animals/sheep.lua
- client/animals/chickens.lua
- client/objectloader.lua (new file)
- server/api.lua
- server/animals/chickens.lua
- server/animals/_shared.lua
- config/objectloader (new directory folder)
- config/objectloader/ranch_1.lua (new file)
- config/config.lua
- config/config_animals.lua
- fxmanifest.lua

## Notes

1. Add in the configuration file for the objectloader feature at `config/config.lua` the following options:

```lua
-----------------------------------------------------------
--[[ Object Loader ( version 1.0.6 ) ]]--
-----------------------------------------------------------

-- Set to false to disable objectloader in case you are interested using ymaps or xmls.
Config.ObjectLoader = true

Config.Objects = {} -- DO NOT TOUCH

-- The wait time for checking nearby objects.
Config.ObjectLoaderWaitTime = 2000 -- Time in milliseconds.

-- DO NOT TOUCH
function InsertObjectData(data)
  table.insert(Config.Objects, data)
end
```

<img width="1287" height="863" alt="image" src="https://github.com/user-attachments/assets/7164e00e-17bf-474d-8769-10f7a37ba46b" />

2. Add in the configuration file for the new feature to change chicken eggs model with your own desired object the new option at `config/config_animals.lua`

```lua
-- Set egg object name, the default is p_egg01x (we use gator egg because its a bit bigger visually).
Config.ChickenEggObjectName = 's_gatoregg01x' -- version 1.0.6
```

<img width="1210" height="878" alt="image" src="https://github.com/user-attachments/assets/628b2766-459a-43b3-a44d-571c094596a2" />


3. Add the new file on client directory folder called `objectloader.lua`

4. Add the new directory folder on `config` folder called `objectloader` and its contents.
  
5. Add the new file on server directory folder called `api.lua`


## How to update?

- You can easily update the modified files by redownloading from the **Cfx.re Portal** and copy-paste on the mentioned directory file paths.
- If new options have been implemented to the configuration file or edited, **DO NOT** copy-paste the new configuration file, otherwise you will get the latest configuration but you will lose all the changes you have made. The proper way updating a configuration file is by finding the new configuration options or the modified ones and do it **manually**.

*Cfx.re Portal **DOES NOT** force-update your configuration file or any files, if it did, you would always lose all the configuration changes you made or would totally break the script, all files are updated or edited manually.*
