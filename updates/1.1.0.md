# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Fixed an issue with water barrel capacity, it could go above 100% capacity.
2. Added a new animal (pig) which does not have any actions, this is useful only for server owners who want to use different type of animals for different meat types.
3. Added an option for goats and cows related to milking, you can now disable or enable the male cows or goats to be milked or not.
4. Added a new option for ranches to allow the owners to invite / add as members on a ranch only players who have the required job(s) - (Each ranch can have its own job requirements).
5. Added a new option for ranches to add a limit on the milk jug containers to hold a limited amount of liters than infinite (you can also have no limitation, its up to you).

## Modified Files:

- client/*.lua (ALL)
- server/*.lua (ALL)
- locales.lua (check notes)
- config_animals.lua (check notes)
- fxmanifest.lua

## Notes

1. Add the new locales on the `locales.lua` file:

```lua
	['MENU_BREEDING_PIG_TITLE']                 = "Pig Breeding", -- version '1.1.0'
	['MENU_BREEDING_PIG_DESCRIPTION']           = "Select which pig (female and male) to start a breeding with each other. ", -- version '1.1.0'
    ['CANNOT_MILK_ANIMAL_IS_MALE']              = { text = "You cannot milk the specified animal, only females are able to be milked.", duration = 6}, -- 1.1.0
```

<img width="1582" height="770" alt="image" src="https://github.com/user-attachments/assets/35c6b924-3624-4419-be00-199e1bd63b45" />

2. Add on `config_animals.lua` file the (2) new options for cows and goats related to milking (Insert this option on both animals below `AddHungerCapacityOnHaybaleDeliver` option.

```lua
-- Set to false if you don't want the male to be milked but only the female ones.
AllowMaleSexMilkingProduction = true,  -- 1.1.0
```

<img width="818" height="636" alt="image" src="https://github.com/user-attachments/assets/8c5b1980-8a83-4063-b043-3ea957eb5b11" />

3. Add on `config_ranches.lua` on every ranch the new option which is for inviting / adding members with required jobs / not.

```lua
-- Set to false if you want everyone to be able to be added as a member on this ranch,
-- otherwise, add the desired jobs to join when having only the required job(s).
-- For adding a job(s), it requires a table form such as: { "job1", "job2" }
AddMemberRequiredJobs = false, -- version 1.1.0
AddMemberRequiredJobsNotify = { text = 'You cannot add as a member the specified user, this player does not have the required job.', duration = 6}, -- 1.0.0

```

<img width="2127" height="805" alt="image" src="https://github.com/user-attachments/assets/7bb1c145-1e78-44d6-9fa7-650476b3f05a" />

4. Add on `config_ranches.lua` on every ranch the new option which is for milk jug containers limitation.

```lua
-- How much milk should the jug container can hold in total?
MaximumRanchMilkJugContainerCapacity = 100, -- liters - (version 1.0.0)
```

<img width="867" height="1218" alt="image" src="https://github.com/user-attachments/assets/06661788-94be-4854-bff6-9d7c14c4d82c" />

## How to update?

- You can easily update the modified files by redownloading from the **Cfx.re Portal** and copy-paste on the mentioned directory file paths.
- If new options have been implemented to the configuration file or edited, **DO NOT** copy-paste the new configuration file, otherwise you will get the latest configuration but you will lose all the changes you have made. The proper way updating a configuration file is by finding the new configuration options or the modified ones and do it **manually**.

*Cfx.re Portal **DOES NOT** force-update your configuration file or any files, if it did, you would always lose all the configuration changes you made or would totally break the script, all files are updated or edited manually.*
